# PolicyNotesService
PolicyNotesService is a simple .NET Web API for managing policy notes.

# Tech Stack
- .NET (Web API – net8.0 as per build output)  
- C#  
- Entity Framework Core for data access (via `PolicyNotesDbContext`)  
- xUnit / MSTest / NUnit (depending on your test framework)  
- Swagger / OpenAPI for API documentation (via Swashbuckle)

